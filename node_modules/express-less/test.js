var express = require('express'),
    logger = require('morgan'),
    less = require('./');

var app = express();
app.use(logger());
app.use('/css', less(__dirname + '/test/fixtures', { debug: true }));

app.listen(8000);
